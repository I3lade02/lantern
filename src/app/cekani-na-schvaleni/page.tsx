import { PendingApprovalScreen } from "@/features/auth/pending-approval-screen";

export const metadata = {
  title: "Čekání na schválení",
};

export default function PendingApprovalPage() {
  return <PendingApprovalScreen />;
}